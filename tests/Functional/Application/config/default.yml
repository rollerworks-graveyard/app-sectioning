imports:
    - { resource: framework.yml }

parameters:
    app_sections:
        backend:
            prefix: backend/
            host: example.{tld}
            requirements: { id: '\d+', tld: com }
            defaults: { tld: com }
        frontend:
            prefix: /
            host: null
